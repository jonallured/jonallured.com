#!/bin/bash

input_path=$1
width=$2
output_folder=$(dirname $input_path)/$width

rm -rf $output_folder
mkdir -p $output_folder

magick $input_path -filter Lanczos -resize ${width}x -strip -sampling-factor 4:2:0 -quality 85 -interlace Plane $output_folder/optimized.jpg
